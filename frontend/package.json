{
  "name": "frontend",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build:css": "tailwindcss -i src/css/tailwind.css -o build/css/styles.css",
    "build": "npm run build:css && mkdir -p build && cp -r src/js build/ && cp src/*.html build/",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "tailwindcss": "^4.1.4"
  }
}